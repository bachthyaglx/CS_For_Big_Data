# Mean (average) is the sum of each item * its fre / sum of fre of each item, median is the middle value
# Mean (average): SUM[(max(i)-min(i))/2]*f(i)]/SUM(f(i)) = 453,073
# Median: l + [(n/2 - c)/f]*h = 60.714

